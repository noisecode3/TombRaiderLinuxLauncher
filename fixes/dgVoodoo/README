# lets say you put DDraw.dll and dgVoodooCpl.exe in:
cd $HOME/.local/share/TombRaiderLinuxLauncher

# lets say you level id is 3377:
ln -s $HOME/.local/share/TombRaiderLinuxLauncher/DDraw.dll $HOME/.local/share/TombRaiderLinuxLauncher/3377.TRLE/

# dgVoodoo2 81.3 saves config in the wineprefix
PROTONPATH="$HOME/.steam/steam/compatibilitytools.d/GE-Proton10-28" umu-run dgVoodooCpl.exe

# Now run them all with umu-run
PROTONPATH="/home/fullUserName/.steam/steam/compatibilitytools.d/GE-Proton10-28" WINEDLLOVERRIDES="ddraw=n" MESA_SHADER_CACHE="1"
# For some reason overiding D3DImm.dll like this WINEDLLOVERRIDES="ddraw=n;d3dimm" just for the setup window

# Create or destroy the link to the DDraw.dll next to the exe file as you like
